# ROGII Results - 2026-08-04

## Generalization-first evidence refresh

The public leaderboard covers roughly 26% of the hidden test distribution,
about 50 wells according to the latest Discussion.  Recent competitors report
large leaderboard swings among near-identical folds, and explicitly recommend
validation trust, whole-model diversity and a robust ensemble insurance route
instead of selecting a single fold or public-score micro-variant.  Discussion
also confirms that the nine-hour limit applies to the full rerun and that a
fixed visible row-count assertion can fail when hidden test size changes.

The current Code scan did not expose a verifiable new safe champion. Physics
v48 appeared first in the Kaggle CLI `scoreAscending` response, but the API
object contains no score field, so that ordering is not evidence that it has
the best public score. Its artifact is nevertheless a duplicate of the already
audited V4/Q-family output, and the current V4 reproduction measured `6.504`
on this account as ref `55208568`. Recent unsafe, duplicate, extreme-distance,
and historically weak outputs were not allocated a slot.

## Updated D36 slate

| Slot | Route | Generalization role | State |
|---:|---|---|---|
| 1 | Raunak Stack full source | multi-model trajectory stack | ref `55225251` pending |
| 2 | Final Hierarchy full source | independent hierarchical PF/branch stack | ref `55225757` pending |
| 3 | AkiiroLabs full source | independent model/artifact ensemble | ref `55226347` pending |
| 4 | Frontier Blend full source | cross-branch robust ensemble | ref `55227570` pending |
| 5 | Shift275 full source | bounded global calibration stress test | ref `55228301` pending |

Shift275 was initially demoted to the fallback because a fixed global
calibration probe is weaker generalization evidence than the validation-first
Harshini route. Harshini remained running for more than two hours at the final
slot window. Scaling a roughly 26% public run to the complete hidden rerun
approached the nine-hour limit, so it failed the deployment-runtime decision
gate and was not competition-submitted. The already audited Shift275 private
Version 1 was therefore used as the fail-safe final route.

## Safety state

All formal competition refs are separated by at least 30 minutes.  Every
candidate must use its own private Version 1, dynamic official sample row count
and exact ID order, finite predictions, clean logs, and an exact remote output
SHA recheck.  No candidate mounts another notebook or public `submission.csv`
as its hidden prediction parent.  Public output files are read only for
research, provenance, duplicate detection, and vector-distance analysis.

AkiiroLabs private Version 1 completed and passed the full gate with 14,151
ordered unique IDs, finite predictions, no fatal log marker, no kernel source,
and output SHA-256
`2ebc68819f77feb407e5bb854906cf10b90886f7c47026e5420b02588e715fec`.
Its RMS distance from the other four eligible outputs is `1.109--1.824 ft`,
which is material but controlled.  The Harshini source has no dataset or
kernel inputs, uses the current competition train/test files, merges onto the
dynamic official sample, and contains no fixed 14,151-row assertion.

The five UTC timestamps are `01:01:16.617 / 01:31:53.737 / 02:02:29.937 /
03:08:32.327 / 03:39:23.840`. Adjacent gaps are
`30:37.120 / 30:36.200 / 66:02.390 / 30:51.513`, all above the pre-registered
30-minute minimum. The daily budget is final at exactly `5 / 5`. All five refs
are currently pending without an error marker; pending is not described as a
measured score. The first Frontier attempt was stopped
locally by an SSL EOF during the remote private-status check, before any
competition ref was created. A fresh remote check passed and created the sole
Frontier ref `55227570`; the transport interruption consumed no budget.
