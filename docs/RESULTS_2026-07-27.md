# ROGII Results - 2026-07-27

## Submission budget

The Kaggle date began at exactly `0 / 5`. One slot is reserved for an exact
current-source reproduction of the highest currently verifiable public Code
claim. The other four slots are reserved for hidden-run-compatible structural
experiments rather than another visible-output micro-grid.

The daily budget is now final at exactly `5 / 5`. All five refs are pending
Kaggle scoring; no additional D27 competition submission is permitted.

| Slot | Private Code | Controlled hypothesis | Status | Ref | Score |
|---|---|---|---|---|---|
| 1 | `rogii-d27-frontier-ii-exact-repro` v1 | exact Frontier II current-source reproduction | pending | `55015407` | pending |
| 2 | `rogii-d27-residual-postcontact-r1` v1 | grouped-OOF learned trajectory at 85%, applied after the contact guard | pending | `55015697` | pending |
| 3 | `rogii-d27-beam-posterior-no-contact` v1 | widened heel-calibrated GR datum posterior without contact overwrite | pending | `55015700` | pending |
| 4 | `rogii-d27-adaptive-branchgate-r1` v1 | row-adaptive SP45/learned weighting from run-local disagreement | pending | `55015791` | pending |
| 5 | `rogii-d27-contact-sixform-r2` v1 | six-formation geometric consensus gated by visible-prefix error | pending | `55015938` | pending |

No candidate may consume a slot until its own private Version 1 has exactly
14,151 sample-ordered unique IDs, finite predictions, a matching final audit
SHA-256, no fatal log marker, and a material non-duplicate output.

## Slot 1 exact-reproduction audit

Prvsiyan's
`rogii-public-score-frontier-ii-visuals` is the current highest public Code
claim that could be tied to a complete downloadable source and output. Its
downloaded artifact claims `6.390`, but this account does not inherit that
score. The source applies a hidden-run-compatible signed `+0.196 ft` extension
to every PF branch that passes its run-local mass and separation gates.

The private Version 1 reproduced the public artifact byte-for-byte:

- rows: `14,151`;
- ordered unique IDs: yes;
- finite predictions: yes;
- SHA-256:
  `bf48603db1b8ab3406a525ef7ef1a3b77424075de1c740f1af8c4cbec8feadd8`;
- RMS from D25 A27 `15/7`: `0.170344693 ft`;
- RMS from cap 2: `0.108055587 ft`;
- RMS from Q0522: `0.179725109 ft`;
- fatal log markers: none.

The visible run selected one 4,301-row PF branch and retained the source
direction dynamically. This is a calibration reproduction, not evidence that
the source-team score will transfer to this account.

## Structural output audits

| Route | File SHA-256 | RMS from D25 A27 | RMS from slot 1 |
|---|---|---:|---:|
| residual post-contact | `0a7c4348b232e3db23402116580c52f1cfc9db4aeefcf874c572b4c4ec47eaca` | `5.275397 ft` | `5.300982 ft` |
| beam posterior, no contact | `4c364a6bd84604217b902951964e991228baa5af00daad6bbe062856186fa83b` | `3.306169 ft` | `3.334922 ft` |
| adaptive branch gate | `685dd480c9c3990adebb84b4001615b202714c55e75853e420f7c78101f8db56` | `3.031769 ft` | `3.060086 ft` |
| six-contact consensus | `503921810a2cb7608aa36429aec4b28217e7b97d373a081d5bb86338a652e23f` | `0.989646 ft` | `1.088901 ft` |

All files have exactly 14,151 sample-ordered unique IDs, finite values, a
matching embedded final-audit SHA, and no fatal log marker. They are
pairwise at least `1.293662 ft` apart, so none is a duplicate or micro-variant.
The visible beam run activated one posterior branch with a `+6.624 ft` datum
shift derived from its own heel-calibrated GR scan. The adaptive gate used
SP45 weights from `0.05` to `0.85` with a run-local mean of `0.3968`. The
contact route passed its pre-registered A27-distance floor by `0.020646 ft`
and remained at least `3.325162 ft` from the other three structural outputs.

## Why the other four routes are structural

This competition reruns Code on hidden wells. Historical notebooks with the
same visible output SHA have produced different public scores, so static
visible-vector score geometry is not a valid hidden-model optimizer. The D27
structural additions therefore derive all decisions from run-local components
and contain no visible well IDs in their added scoring transactions.

1. **Residual post-contact:** replaces a successful contact reconstruction
   with an 85% grouped-OOF learned trajectory and 15% SP45/PF anchor. Applying
   it after the guard prevents the contact path from making the experiment
   byte-inert.
2. **Beam posterior without contact overwrite:** widens the GR datum scan,
   uses heel calibration and prefix trust, and keeps posterior branch
   uncertainty. The contact guard is deliberately disabled so the state-space
   hypothesis can reach the final file.
3. **Multi-contact consensus:** combines ANCC, ASTNU, ASTNL, EGFDU, EGFDL, and
   BUDA surfaces using inverse prefix-error weights, a robust prefix-only datum
   correction, and a bounded move.
4. **Uncertainty-gated ensemble:** gives the grouped-OOF learned path more
   weight when it agrees with SP45/PF and falls back toward the physical path
   under large run-local disagreement.

The target `5.5` is a research objective, not a promised score. Any route that
finishes byte-identical to an earlier candidate, fails its output contract, or
cannot express a material hidden-compatible change is rejected without using a
competition submission.

## Pre-submission rejection

The first residual-heavy implementation changed the learned/SP45 blend before
the guarded contact reconstruction. Its private run completed, but the contact
layer overwrote all three visible wells and the final output was byte-identical
to slot 1 (the same `bf48603d...add8` SHA and `0.000000 ft` RMS distance).
It was rejected without submission. The corrected residual experiment is a new
private Version 1 under a new slug and executes after the contact guard.

The first post-dynamic contact-consensus draft used a `0.85` blend. Local
exact-vector preview placed it only about `0.93 ft` from D25 A27, below the
pre-registered `0.969 ft` structural floor, so it was also rejected without a
competition submission. R2 fixed the blend at `0.90` before execution and
measured `0.989646 ft` from A27.
